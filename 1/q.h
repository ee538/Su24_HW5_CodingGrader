#include <iostream>
#include <string>
#include <list>
#include <vector>

// TODO: Implement the following functions in q.cc.

// Given an integer n, Implement a function that calculates the
// n(th) Fibonacci number.
// The first number starts from 1 instead of 0: {1, 1, 2, 3, 5, ...}.
// Return 1 if the input is a negative number.

// Your implementation should have a worst case runtime complexity of O(n) and
// memory complexity of O(1).
int Fibonacci(int n);
